# Google Calendar Configuration
# Copy this file to .env and fill in your values

# Path to the Google Service Account credentials JSON file
GOOGLE_CREDENTIALS_FILE=credentials.json

# Your Google Calendar ID
# For primary calendar, use 'primary'
# Or use the calendar's email address (found in Calendar Settings > Integrate calendar)
GOOGLE_CALENDAR_ID=primary

# Flask secret key (change this to a random string in production)
FLASK_SECRET_KEY=your-secret-key-here

# ============== EMAIL CONFIGURATION ==============
# For Gmail: Enable 2FA and create an App Password
# https://myaccount.google.com/apppasswords

SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your-email@gmail.com
SMTP_PASSWORD=your-app-password
